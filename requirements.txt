langgraph>=0.2.0
langchain-core>=0.3.0
rank-bm25>=0.2.2
scikit-learn>=1.3.0
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
gradio>=4.0.0

# --- optional, enabled by config (not required for sample/mock mode) ---
# langchain-groq>=0.2.0          # MODEL_PROVIDER=groq
# langchain-anthropic>=0.2.0     # MODEL_PROVIDER=anthropic
# langchain-openai>=0.2.0        # MODEL_PROVIDER=openai
# sentence-transformers>=2.2.0   # RAG_EMBEDDINGS=real
# DATA_MODE=live needs no extra deps — EDGAR + FMP/AlphaVantage use stdlib urllib.
# Just set the keys in .env (FMP_API_KEY or ALPHAVANTAGE_API_KEY, SEC_USER_AGENT).
